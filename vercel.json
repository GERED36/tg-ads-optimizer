{
  "buildCommand": "npm run db:push && cd dashboard && npm run build",
  "outputDirectory": "dashboard/dist",
  "installCommand": "npm install && cd dashboard && npm install",
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api" },
    { "source": "/(.*)", "destination": "/index.html" }
  ],
  "functions": {
    "api/index.ts": {
      "maxDuration": 30
    }
  }
}
